# مدیریت فروشگاه و گارانتی — نسخه اصلاح‌شده

- شماره‌های ۱ تا ۳۰ از کنار کدها حذف شده‌اند.
- کد `78905` دقیقاً `78905` ذخیره و نمایش داده می‌شود.
- بخش مستقل فروشگاه/شخص اضافه شده است.
- اطلاعات فروشگاه یک‌بار ثبت و برای گارانتی‌های بعدی فقط با انتخاب فروشگاه استفاده می‌شود.
- هر ثبت گارانتی شامل تعداد ۲۰ تا ۳۰ کارت، نوع کارت، تاریخ و کدهاست.
- اطلاعات در LocalStorage دستگاه ذخیره می‌شود.
- خروجی CSV و پشتیبان JSON دارد.
- دکمه واتساپ متن را کپی کرده و لینک گروه را باز می‌کند؛ ارسال خودکار به گروه از داخل GitHub Pages توسط محدودیت واتساپ ممکن نیست.
